<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.erhuo.activitiy_erhuo">

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="贰货" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".MySellingCommodity" />
        <activity android:name=".SellCommodityEdit" />
        <activity android:name=".SellCommodityReEdit" />
        <activity android:name=".MyRequiringCommodity" />
        <activity android:name=".RequireCommodityEdit" />
        <activity android:name=".SellingDetail" />
        <activity android:name=".Me" />
        <activity
            android:name=".Login"
            android:label="贰货">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.example.cameraalbumtest.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <activity android:name=".NoticeActivity" />
        <activity android:name=".MyRecordActivity" />
        <activity android:name=".CollectionActivity" />
        <activity android:name=".Registry"></activity>
    </application>

</manifest>